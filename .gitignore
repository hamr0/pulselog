# dependencies
node_modules/

# generated types (built on publish per LIBRARY_CONVENTIONS)
/types/

# packed tarballs
*.tgz

# npm debug logs
npm-debug.log*

# runtime error logs (incl. POC output)
*.jsonl

# env
.env

# local working state (stash + machine-local settings; memory/*.md are tracked)
.claude/stash/
.claude/settings.local.json

# litectx impact-analysis cache (local tooling)
.litectx/

# Agent / IDE scratch — never commit (per hamr0 LIBRARY_CONVENTIONS §7)
.claude/
.litectx/
.idea/
